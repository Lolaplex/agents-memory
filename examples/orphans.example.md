# Orphans

Rogue dump files and pre-taxonomy singles land here (`~/.agents/memory/orphans/`).

Distill each bullet into a typed path (`concepts/`, `notes/<collection>/`, `<repo>/.agents/memory/...`), then delete the orphan file.

Do not create `facts.md` or other monolithic dumps.
